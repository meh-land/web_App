import { FC } from "react";
import Card from "../Card/Card";

const LiveLocation: FC = () => {
  return <Card Title="Live Location" Content={<h1>Vedio Stream</h1>} />;
};

export default LiveLocation;
